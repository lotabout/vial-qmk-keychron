#pragma once

// Temporary UID; replace with real unique values per board before upstreaming
#define VIAL_KEYBOARD_UID {0xA2, 0xC1, 0x6B, 0x30, 0x11, 0x7E, 0x9D, 0x45}

// Unlock combo: Esc (row0,col0) + B (row4,col5) — adjust if desired
#define VIAL_UNLOCK_COMBO_ROWS { 0, 4 }
#define VIAL_UNLOCK_COMBO_COLS { 0, 5 }
